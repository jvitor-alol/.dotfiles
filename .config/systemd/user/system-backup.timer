# ~/.config/systemd/user/system-backup.timer

[Unit]
Description=Run system config backup daily

[Timer]
OnBootSec=10m
OnCalendar=daily
AccuracySec=10m
Persistent=true

Unit=system-backup.service

[Install]
WantedBy=default.target
